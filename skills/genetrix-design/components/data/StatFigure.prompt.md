**StatFigure** — the brand's "le chiffre d'abord": a key figure set large in red with an uppercase label. Use `prefix`/`suffix` for "+", "%".

```jsx
<StatFigure prefix="+" value="100" label="organisations accompagnées" />
```
