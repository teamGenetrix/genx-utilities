**Tooltip** — dark hover/focus tooltip wrapping a trigger. `placement` top/bottom/left/right.

```jsx
<Tooltip label="Exporter en PDF"><IconButton aria-label="Export"><Download size={18} /></IconButton></Tooltip>
```
