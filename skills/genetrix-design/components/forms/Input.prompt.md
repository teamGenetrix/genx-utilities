**Input** — text field with optional `label`, `hint`, `error`, `prefix`/`suffix`. Red focus ring; error state uses brand red.

```jsx
<Input label="Email professionnel" placeholder="vous@entreprise.ci" />
<Input label="Nom" error="Ce champ est requis" />
```
