**IconButton** — square, icon-only action (menu, close, nav arrows). Requires `aria-label`.

```jsx
<IconButton aria-label="Fermer" variant="ghost"><X size={18} /></IconButton>
```

Same variants as Button; sizes map to 32/40/48px.
