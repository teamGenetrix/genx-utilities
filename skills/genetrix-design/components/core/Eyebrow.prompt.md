**Eyebrow** — the uppercase, letter-spaced overline that labels a section, with an optional numbered prefix and red accent.

```jsx
<Eyebrow number="01">L'idée directrice</Eyebrow>
```

Set `onDark` on noir backgrounds.
